import { Meta } from '@storybook/addon-docs';
import { Story } from '@storybook/blocks';
import '../../../style/style.scss';

import ColorPalette from './ColorPalette.stories';

<Meta title="Foundation/Color System" />

<div className="position-top">
  <div className="pt-2 box-shadow-me">
    <div className="container">
      <div className="row justify-content-center">
        <div className="col-10 content-section">
            <h1 className="h2">Color</h1>
            <p>colors work in many ways with the Stellar Kit. They communicate how things function in the interface. This helps us create visual patterns that can make interacting with our product easier.</p>
            <p>The color system starts with a basic naming structure, naming the color. We've also setup an alias naming structure for contextual use of the colors rather than just the HEX value.</p>

            <h2 className="mt-4">Color Palette</h2>
            <p>Stellar Kit contains two color palettes. Our primary palette for our brand and neutral colors. And a secondary palette to provide more information about our product with the help of semantic colors in green, yellow and red.</p>
            
            <h2 className="h4 mt-4">Primary Palette</h2>
            <h3 className="h5 mt-3">Blue</h3>
            <p>We use our brand color blue for primary actions in elements such as buttons, text links and background banners. This eye catching color will help draw the user's attention to the important pieces of information contained inside the page.</p> 
            <Story id="foundation-tokens-color--blue" />

            <h3 className="h5 mt-3">Neutrals</h3>
            <p>Neutrals are used for text and subtle backgrounds when we don't want to draw too much attention to a particular area or inform that objects are <em>"disabled"</em> or <em>"inactived"</em>. Similar degrees of saturation are used for borders in elements and buttons.</p>
            <Story id="foundation-tokens-color--neutral" />
            

            <h2 className="h4 mt-4">Secondary Palette</h2>
            <h3 className="h5 mt-3">Green</h3>
            <p>Green will represent a success state in our semantic color palette. When used in illustration it will carry a message of positivty and good news.</p>
            <Story id="foundation-tokens-color--green" />

            <h3 className="h5 mt-3">Yellow</h3>
            <p>Yellow indicates a warning or that progress is impeded.</p>
            <Story id="foundation-tokens-color--yellow" />

            <h3 className="h5 mt-3">Red</h3>
            <p>Red is used as a semantic error state. This will draw attention to actions that are blocking or failing the flow of the page. When displayed in messages or banners, this color will indicate an issue or problem with one piece of our system. Red will also carry negative connotation when used as a color of choice for illustrations or typography.</p>
            <Story id="foundation-tokens-color--red" />

            <div className="tip-wrapper">
              <span className="tip">Tip</span> Edit the Markdown in <code>Storybook/stories/foundation/Color/Color.stories.mdx</code>
            </div>
        </div>
      </div>
    </div>
  </div>
</div>